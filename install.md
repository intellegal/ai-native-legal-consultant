# 给 Codex：准备工作区并进入法律咨询

本页写给处在空白任务中的 Agent。用户说“读本页，帮我进行配置，我需要法律咨询”或表达同等意思时，直接执行；不要反问“要配置什么”。这里的“配置”只指准备一份本地法律咨询工作区，不代表配置 MCP、索取 API Key、安装个人 Skill 或改动用户的全局 Codex 环境。

用户不应被要求理解下载、解压、Git、Skill、MCP、目录结构或工作区配置。正常路径只需要用户确认一次存放位置，必要时批准文件或网络访问，随后点击链接进入咨询。

## 目标与边界

1. 先让用户确认工作区存放位置，再创建任何目录。
2. 从官方仓库取得当前产品包并在最终目录验证。
3. 通过 `codex://new?path=...` 打开以该目录为主文件夹的新本地 Project 任务。
4. 安装任务在交接后结束；咨询、材料处理和法律研究只能在已绑定的新任务中进行。

普通安装走官方 ZIP，不需要预先盘点或安装 Git、GitHub CLI、Node、npm、Python、Docker、MCP 或元典。只有 ZIP 实际失败并需要回退时，才检查与该回退直接相关的能力。

## 第一步：确认存放位置

先识别操作系统并解析系统“文档”目录：

- Windows：系统解析出的“文档”目录；
- macOS：用户的 `Documents` 目录；
- Linux：优先使用 XDG 文档目录，否则使用用户的 `Documents` 目录。

在其下建议一个尚不存在的目录：`我的法律咨询/咨询-YYYYMMDD-HHmm`。第一次回复只问一个具体问题：

> 这次咨询会在本地保存你的材料整理、工作底稿和分析报告。建议放在：`<绝对路径>`。是否使用这个位置？你也可以直接告诉我另一个文件夹。

不要在用户回答前创建、下载或复制文件。用户说“可以”“默认”“随意”“你决定”或同等意思，即视为同意建议位置；用户给出其他路径时使用该路径。若用户在最初请求中已经明确了最终路径，则复述该绝对路径并直接继续，不重复询问。

除非用户明确选择的是一份已经安装且将按下文验证的本产品工作区，目标目录必须是新建的空目录。同名、已存在或非空时，提出一个带安全后缀的新路径并确认；永不覆盖、合并、删除或重用非空目录。若最终位置明显属于 OneDrive、iCloud、Dropbox 等同步目录，创建前用一句话提醒同步副本风险，但不要朗读完整数据说明。

## 第二步：取得产品包

只有位置确认后才创建目标目录。

1. 如果用户指定的目标目录已经包含以下四项，不重复安装，直接进入验证：
   - `AGENTS.md`
   - `.agents/skills/voice-legal-consultation-cn/SKILL.md`
   - `.agents/skills/prc-case-research/SKILL.md`
   - `.agents/release.json`

2. 否则优先在临时目录下载官方仓库 `main` 的源代码压缩包：

   `https://github.com/intellegal/ai-native-legal-consultant/archive/refs/heads/main.zip`

   自动解压，只把压缩包内仓库根层的内容复制到目标目录，再清理临时文件。不得让用户自己访问网页、下载或解压；不得使用第三方镜像。

3. 只有官方 ZIP 因仓库权限或网络策略确实失败时，才检查用户现有的 Git/GitHub 认证并尝试非交互式浅克隆。不要为这条回退预先安装工具。两种方式都失败时，只说明“官方仓库访问权限或网络授权”这一项阻塞，并给一个当前可执行动作；不要改从不明来源下载，也不要把一整套手工攻略丢给用户。

## 第三步：验证与绑定

在最终目录重新检查四个核心文件，并验证：

- `.agents/release.json` 是有效 JSON；
- 其中 `repository` 精确等于 `https://github.com/intellegal/ai-native-legal-consultant`；
- 最终目录根层就是 `AGENTS.md` 所在层，没有多套一层压缩包目录；
- `.codex/config.toml`、真实 Key、真实用户材料、工作底稿和咨询结果没有随发布包出现。

读取并记录本地版本。任一项失败都停止并指出具体缺项，不得声称准备成功。

这里的“项目绑定”按以下事实判断：

1. 深链接的 `path` 必须是上述最终目录的绝对路径；
2. 新咨询任务的主文件夹/工作目录必须是该路径，Codex 才能自动发现根目录 `AGENTS.md`、`.agents/skills/` 和项目配置；
3. 如果用户日后移动了文件夹，原 Project 不会因此自动改绑，必须重新打开新路径，或在“编辑项目”中把新路径设为主文件夹；
4. 任何准备输出实体法律判断的 worker 都应先确认当前项目根含上述四项。若不满足，只处理重新绑定，不开始咨询或研究。

安装阶段不运行项目内 MCP 配置脚本，不索取元典 Key，不修改个人 Codex 配置、个人 Skills 或全局 MCP。法规与案例核查由已绑定项目在用户需要时再引导。

如果用户已经在安装任务中披露了具体案件事实，不要求重说。验证完成后，把原话保存到新项目的 `002_你的材料/001_首次描述.md`，标明“来自安装任务中的用户陈述，尚未核实”；不得把安装 Agent 的推断混入原话。

## 第四步：交接到正确任务

对最终目录绝对路径和启动语分别做 URI 百分号编码，返回可点击的 Markdown 链接：

```text
codex://new?path=<编码后的绝对路径>&prompt=<编码后的启动语>
```

启动语固定为：

> 我需要法律咨询

链接文字固定为“进入法律咨询”。如果宿主已有安全、受支持的方式打开本地 `codex://` 协议，可以直接打开；否则返回链接。官方深链接只打开正确路径并预填文字，不替用户确认信任或发送。

交接信息保持简短，但必须包含以下操作：

1. 点击“进入法律咨询”，核对打开位置就是已显示的最终绝对路径，确认信任后发送“我需要法律咨询”。
2. 若要完整语音交互，可使用界面中的 GPT Live/开始 Voice 入口。即使文字任务已经开始，宿主也可能再打开一个带 GPT Live 动态界面的独立语音窗口；这是完整 Voice，不是单次听写。进入后说“继续当前法律咨询”，让它协调这个已经绑定的咨询任务。
3. 如果麦克风只把语音转成输入框文字、仍需手动发送，那是语音听写，不是完整 GPT Live。两种入口都能输入信息，但只有完整 Voice 窗口负责实时对话。

GPT Live 窗口可以与 Project 任务分开显示；可靠性取决于它所协调的 worker 是否绑定到最终路径，而不是两个窗口是否长得一样。安装 Agent 不得让 Voice 新建一个没有产品文件的通用空目录，也不得在自己的临时安装目录继续法律采访。

若深链接不可用，保留一行最终绝对路径，并只给一个兜底动作：在 Codex 中把该路径作为本地项目打开。不要要求用户理解内部目录、阶段、状态或下载命令。

交接完成后，本安装任务即终止。不要在本任务调用咨询 Skill、MCP 或案例研究；后续所有工作都发生在新项目任务中。
